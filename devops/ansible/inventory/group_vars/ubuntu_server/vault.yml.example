# Example vault file for ubuntu_server group
# Create actual vault file with:
# ansible-vault create ~/.ansible/inventory/group_vars/ubuntu_server/vault.yml
#
# Then add these sensitive variables (encrypted):

DB_PASSWORD: "your_secure_database_password"
JWT_SECRET_KEY: "your_jwt_secret_key_min_256_bits"
